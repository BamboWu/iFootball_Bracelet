This directory contains head file of ARM core and startup assemble code.  
And Makefile.common are project template for gcc compiler.  
And \*.ld are files used by gcc linker to specify address of flash region.
